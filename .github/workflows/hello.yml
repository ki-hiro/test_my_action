on: push

jobs:
  hello-world:
    runs-on: ubuntu-latest
    name: Greeting
    steps:
      - name: Checkout
        uses: actions/checkout@v1
      - name: Hello
        uses: ./
        with:
          param1: Hello
          param2: World

